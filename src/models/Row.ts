export type Row2 = {
  part_id: string;
  row_start: number;
  amount_of_rows: number;
  instructions: string;
};

export type Row = {
  row_start: number;
  amount_of_rows: number;
  instructions: string;
};
